.fullscreen-image-container {
  position: fixed; /* 固定定位，使其相对于视口定位 */
  top: 0;
  left: 0;
  width: 100%;
  height: 100vh; /* 视口高度，确保占满屏幕高度 */
  overflow: hidden; /* 隐藏超出容器的部分 */
}

.fullscreen-image {
  width: 100%;
  height: 100%;
  object-fit: cover; /* 让图片适应容器并覆盖整个容器，保持纵横比 */
}